# <Feature title> — Tasks

Each task links to the goal in proposal.md. Work the frontier (tasks whose blockers are done);
commit per task with its ID.

- [ ] **T1** — <task>. _Goal: <one line, why this task serves proposal.md's goal>._
  Seam: <endpoint / function / command from design.md>. Blocked by: none.
- [ ] **T2** — <task>. _Goal: ..._
  Seam: .... Blocked by: T1.
